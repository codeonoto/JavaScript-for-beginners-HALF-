const printHello = () => console.log("hello !");

// sum Function
function mySum(a, b) {
  return a + b;
}

// Arrow Function Can be stored in varibles..!

const arrowSum = (a, b) => {
  console.log(a + b);
};

arrowSum(2, 3);
